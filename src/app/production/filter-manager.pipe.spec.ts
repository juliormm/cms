import { FilterManagerPipe } from './filter-manager.pipe';

describe('FilterManagerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterManagerPipe();
    expect(pipe).toBeTruthy();
  });
});
