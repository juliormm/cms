import { PriorityQueuePipe } from './priority-queue.pipe';

describe('PriorityQueuePipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityQueuePipe();
    expect(pipe).toBeTruthy();
  });
});
