import { CreativesByTypePipe } from './creatives-by-type.pipe';

describe('CreativesByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CreativesByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
