import { EllipsisTextPipe } from './ellipsis-text.pipe';

describe('EllipsisTextPipe', () => {
  it('create an instance', () => {
    const pipe = new EllipsisTextPipe();
    expect(pipe).toBeTruthy();
  });
});
